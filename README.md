## How to use

Ensure Python 3.12+ is installed.

In your terminal, go to the top level directory `Keylog/`, then run `pip install -e .`. this will install the script as a package.
From there, you can run `keylog` as a command to run the script globally.
